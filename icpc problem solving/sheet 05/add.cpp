#include<bits/stdc++.h>
using namespace std;
#define fast ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
int sum (int a, int b)
{
    return a + b;
}

int main() {
    fast;
    int a, b;
    cin >> a >> b;
    int result = sum (a, b);
    cout << result << "\n";

    
    return 0;
}