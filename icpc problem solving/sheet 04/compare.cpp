#include<bits/stdc++.h>
using namespace std;
#define fast ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

int main() {
    fast;
    string x, y;
    cin >> x >> y;
    if (x < y)
    {
      cout << x << '\n';
    }
    else
    {
      cout << y << '\n';  
    
    }
      
    return 0;
}
